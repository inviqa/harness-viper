import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import FieldRenderer from './FieldRenderer';
import * as mocks from './mocks';

<Meta title="Molecules/FieldRenderer" component="FieldRenderer" />

# FieldRenderer

Use it to render a field based on a component information.

# Preview

## default (with label)

<Preview>
  <Story name="default (with label)">
    <FieldRenderer {...mocks.defaultWithLabel} />
  </Story>
</Preview>

## default (without label)

<Preview>
  <Story name="default (without label)">
    <FieldRenderer {...mocks.defaultWithoutLabel} />
  </Story>
</Preview>

## default inline (without label)

<Preview>
  <Story name="default inline (with label)">
    <FieldRenderer {...mocks.defaultWithLabel} inline />
  </Story>
</Preview>

## default inline (without label)

<Preview>
  <Story name="default inline (without label)">
    <FieldRenderer {...mocks.defaultWithoutLabel} inline />
  </Story>
</Preview>

## Title

<Preview>
  <Story name="Title">
    <FieldRenderer {...mocks.title} />
  </Story>
</Preview>

## Image

<Preview>
  <Story name="Image">
    <FieldRenderer {...mocks.image} />
  </Story>
</Preview>

## HTML

<Preview>
  <Story name="HTML">
    <FieldRenderer {...mocks.html} />
  </Story>
</Preview>

## Author

<Preview>
  <Story name="Author">
    <FieldRenderer {...mocks.author} />
  </Story>
</Preview>

## Created at

<Preview>
  <Story name="CreatedAt">
    <FieldRenderer {...mocks.createdAtWithLabel} />
  </Story>
</Preview>
