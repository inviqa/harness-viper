import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import Header from './Header';
import { withApollo } from '@inviqa/viper-storybook-addons';
import { getCartMock } from '~hooks/apollo/mocks/GetCart';
import { getMenuMock } from '~hooks/apollo/mocks/GetMenu';
import { getCheckoutMock } from '~hooks/apollo/mocks/GetCheckout';

<Meta
  title="Organisms/Header"
  component="Header"
  decorators={[withApollo]}
  parameters={{
    apollo: {
      mocks: [getMenuMock({ name: 'main' }), getCartMock(), getCheckoutMock()]
    }
  }}
/>

# Preview

<Preview>
  <Story name="Header">
    <Header />
  </Story>
</Preview>
