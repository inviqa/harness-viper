type Sort {
  criteria: ProductSortCriteria!
  order: ProductSortOrder!
}

type FilterValueClient {
  eq: String
  in: [String]
  match: String
  from: String
  to: String
}

type Filter {
  name: String!
  value: FilterValueClient!
}

type FacetOption {
  isSelected: Boolean!
}
